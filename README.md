Succeeded in building simple_test.c with CMock. Test passes.

Next step is to test a 4d stencil function. The function returns the mean of four floats passed as floating point
pointers to the function.
